    Leave your comment:
    <% form_remote_for Comment.new,
        :url => { :action => :comment },
        :success => visual_effect(
           :fade,
           "comment_form",
           :duration => 0.6
        ),
        :failure => visual_effect(
           :fade,
           "comment_form",
           :duration => 6.0
        ) do |f| %>
      <%= f.hidden_field :blog_id, :value => id %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <br />
      <%= f.label :email, "Email <i>remains private</i>"%>
      <%= f.text_field :email %>
      <br />
      <%= f.label :website %>
      <%= f.text_field :website %>
      <br />
      <%= f.label :comment %>
      <%= f.text_area :comment, :cols => 80, :rows => 10 %>
      <br />
      <%= recaptcha_tags %>
      <br />
      <%= submit_tag "Say so" %>
    <% end %>
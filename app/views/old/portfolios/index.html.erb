<h1>Listing portfolios</h1>

<table>
  <tr>
  </tr>

<% for portfolios in @portfolios %>
  <tr>
    <td><%= link_to 'Show', portfolios %></td>
    <td><%= link_to 'Edit', edit_portfolios_path(portfolios) %></td>
    <td><%= link_to 'Destroy', portfolios, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New portfolios', new_portfolios_path %>
